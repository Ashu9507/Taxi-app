services:
  app:
    build: .
    container_name: taxi_app
    ports:
      - "8080:8080"
    depends_on:
      - mysql
    environment:
      # Use the service name 'mysql' as the host
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      # These must match the mysql service below
      MYSQL_DATABASE: railway
      MYSQL_USER: root
      MYSQL_PASSWORD: TWlcdNdeajfiOBmyCBEyPrpqtEbrefpM
    restart: "no"

  mysql:
    image: mysql:8.0
    container_name: taxi_mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: railway
      MYSQL_ROOT_PASSWORD: TWlcdNdeajfiOBmyCBEyPrpqtEbrefpM
    volumes:
      - taxi_db:/var/lib/mysql
    restart: always

volumes:
  taxi_db:
